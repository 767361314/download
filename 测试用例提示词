测试用例编写规则：
1. 测试方法：必含功能、异常、边界、兼容性测试，可选性能测试（需标注）；
2. 固定格式：模块+场景+前置条件+测试步骤（≤5步）+预期结果（≤2句）+优先级（高/中/低）；
3. 语言：无歧义、可执行，不冗余；
4. 覆盖：核心功能无遗漏，异常场景含输入非法、操作超时等。

示例：
模块：用户登录；
场景：账号密码正确登录；
前置条件：账号已注册；
测试步骤：1. 打开登录页；2. 输入正确账号密码；3. 点击登录；
预期结果：成功跳转首页，显示用户名；
优先级：高。
